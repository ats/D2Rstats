---
title: "Experiments"
format: html
---

```{r}
library(tidyr)
library(dplyr)
library(ggplot2)
library(ggthemes)
library(gifski)
library(gganimate)
library(glue)


source("parameters.R")
```

```{r}
# load dev version of d2helper
devtools::load_all("~/workspace/d2helper")
```

```{r}
# load data
# load(glue("{membershipId}-data/PGCRdata"))
load_pgcr_data(membershipId)

```



```{r}
levelWeapon |>
  animate_history(activity_type = 82, limit=30, label="Dungeon weapons over time")

levelWeapon |> 
  animate_history(activity_type=19, limit=30, label="Iron Banner kills", prefix="ib")
levelWeapon |>
  animate_history(activity_type="4", label="Raid kills", prefix="raid", gif=TRUE)
levelWeapon |>
  animate_history(activity_type="82", limit=30, label="Dungeon kills", mp4=TRUE)
levelWeapon |>
  animate_history(activity_type="46", limit=30, label="GM NF? kills", prefix="gm", gif=TRUE)
levelWeapon |> 
  animate_history(activity_type = "5", label="All PVP Ever!", gif=TRUE)

```

