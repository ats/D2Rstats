---
title: "Experiments"
format: html
---

```{r}
library(tidyr)
library(dplyr)
library(ggplot2)
library(ggthemes)
library(gifski)
library(gganimate)
library(glue)


source("parameters.R")
```

```{r}
# load data
load(glue("{membershipId}-data/PGCRdata"))
```

```{r}
# load dev version of d2helper
load_all("~/workspace/d2helper")
```


```{r}
levelWeapon |>
  animate_history(activity_type = 82, limit=30, label="Dungeon weapons over time")

levelWeapon |> 
  animate_history(activityType=19, limit=30, label="Iron Banner kills", prefix="ib")
levelWeapon |>
  animate_history(activityType="4", label="Raid kills", gif="raid", av="raid")
levelWeapon |>
  animate_history(activityType="82", limit=30, label="Dungeon kills", av="dungeon")
levelWeapon |>
  animate_history(activityType="46", limit=30, label="GM NF? kills", prefix="gm")
levelWeapon |> 
  animate_history(activityType = "5", label="All PVP Ever!", gif="pvp")


```

